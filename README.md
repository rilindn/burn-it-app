# BurnIT App

## Running Tests

1. Install dependencies (including PHPUnit):
   ```bash
   composer install
   ```
2. Execute the test suite using PHPUnit:
   ```bash
   vendor/bin/phpunit
   ```

The tests cover the `RegisterLogic::dataCorrect` method with both valid and invalid inputs.
